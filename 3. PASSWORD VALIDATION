<?php
$pass = "Test@1234";

if(strlen($pass) >= 8 &&
   preg_match('/[A-Z]/',$pass) &&
   preg_match('/[a-z]/',$pass) &&
   preg_match('/[0-9]/',$pass) &&
   preg_match('/[@$!%*?&]/',$pass))
{
  echo "Strong Password";
}
else {
  echo "Enter complex password";
}
?>
